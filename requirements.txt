# Core
anthropic>=0.40.0
python-dotenv>=1.0.0
rich>=13.0.0
requests>=2.31.0

# HTML parsing (for web research)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Flask (paywall webhook server)
flask>=3.0.0

# Social posting (Reddit)
praw>=7.8.0
