# Core
anthropic>=0.40.0
python-dotenv>=1.0.0
rich>=13.0.0
requests>=2.31.0

# HTML parsing (for web research)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Flask (paywall webhook server)
flask>=3.0.0

# Social posting (Reddit)
praw>=7.8.0

# Media Buyer webhook server (FastAPI) — optional; only needed when running
# run_media_buyer_server.py. Cron usage of run_media_buyer_auto.py works without these.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# PolyMarket Weather Trading Agent
numpy>=1.26.0
xgboost>=2.0.0
py-clob-client>=0.18.0   # optional — only required for live order placement

# ICT Predictor — MetaTrader 5 order submission
# Optional — the agent's price analysis and report generation work without it.
# Only needed to actually submit orders to a running MT5 terminal.
#
# The environment marker is REQUIRED, not cosmetic: MetaTrader5 publishes
# Windows wheels only, so an unconditional entry breaks `pip install -r` on
# every Linux/macOS consumer — including the Netlify build, which installs a
# root requirements.txt automatically and fails the deploy.
# The import is guarded in ict_predictor/mt5_execution.py, so on platforms
# where pip skips this the agent still analyses and reports normally.
MetaTrader5>=5.0.45; sys_platform == "win32"
